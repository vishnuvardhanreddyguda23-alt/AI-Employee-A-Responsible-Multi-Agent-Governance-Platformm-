# Contributing

1. Create a branch.
2. Keep changes focused; do not rewrite working modules without a reason.
3. Run backend tests with `cd backend && pytest -q`.
4. Run frontend checks with `cd frontend && npm ci && npm run build`.
5. Keep secrets out of source control.
6. Update documentation when behavior changes.
7. Add regression tests for security, governance and API changes.
